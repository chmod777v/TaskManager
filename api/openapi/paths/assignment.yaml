paths:
  /assign:
    post:
      tags:
        - Assignment
      summary: Назначить задачу разработчику
      description: Если в качестве ключа 0 то задача назначается на всех доступных пользователях
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '../components/schemas.yaml#/MapId'
      responses:
        '200':
          description: Успешное назначение


  /assign/user/{id}:
    get:
      tags:
      - Assignment
      summary: Задачи конкретного разработчика
      parameters: 
        - $ref: '../components/parameters.yaml#/UserIdRequest'
      responses:
        '200':
          description: Успешный ответ с списком задач
          content:
            application/json:
              schema:
                $ref: '../components/schemas.yaml#/ListId'

    delete:
      tags:
        - Assignment
      summary: Удаление задачи разработчика по его id
      parameters: 
        - $ref: '../components/parameters.yaml#/UserIdRequest'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '../components/schemas.yaml#/ListId'
      responses:
        '200':
          description: Успешное удаление